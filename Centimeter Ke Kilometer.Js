function cmToKm(cm) {
    return cm / 100000;
}

function kmToCm(km) {
    return km * 100000;
}

// Contoh penggunaan
console.log(`100000 cm = ${cmToKm(100000)} km`);
console.log(`1 km = ${kmToCm(1)} cm`);